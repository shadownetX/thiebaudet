language: php
php:
  - "7.2"

services:
  - docker

before_install:
  - npm i -g npm

install:
  - composer install
  - npm install

before_script:
  - npm run-script build

script:
  - bin/docker install

after_script:
  - php ./vendor/bin/php-cs-fixer fix src